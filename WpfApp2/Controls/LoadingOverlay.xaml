<UserControl x:Class="Som3a_WPF_UI.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Storyboard x:Key="SpinAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerTransform"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360" Duration="0:0:1"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid Background="#88000000">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Width="50" Height="50">
                <Ellipse Stroke="{DynamicResource AccentBrush}" StrokeThickness="4" Opacity="0.3"/>
                <Ellipse Stroke="{DynamicResource AccentBrush}" StrokeThickness="4"
                         StrokeDashArray="2,2" StrokeDashOffset="0">
                    <Ellipse.RenderTransform>
                        <RotateTransform x:Name="SpinnerTransform" CenterX="25" CenterY="25"/>
                    </Ellipse.RenderTransform>
                </Ellipse>
            </Grid>

            <TextBlock x:Name="MessageText"
                       Foreground="{DynamicResource TextMainBrush}"
                       FontSize="14"
                       Margin="0,16,0,0"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Loading..."
                       Foreground="{DynamicResource TextSubBrush}"
                       FontSize="12"
                       Margin="0,8,0,0"
                       HorizontalAlignment="Center"
                       Visibility="{Binding Message, Converter={StaticResource NullToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>