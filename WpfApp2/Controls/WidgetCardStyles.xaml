<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Som3a_WPF_UI.Controls"
    xmlns:services="clr-namespace:Som3a_WPF_UI.Services"
    xmlns:fluentIcons="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf">

    <Style TargetType="{x:Type controls:WidgetCard}">
        <Setter Property="Background" Value="{DynamicResource Brush.Background.Card}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Stroke.Card}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="MinWidth" Value="280"/>
        <Setter Property="MinHeight" Value="180"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Effect" Value="{DynamicResource Shadow.Card}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:WidgetCard}">
                    <Border x:Name="CardBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            Effect="{TemplateBinding Effect}"
                            Focusable="True">
                        <Border.InputBindings>
                            <MouseBinding Gesture="LeftClick"
                                          Command="{TemplateBinding Command}"
                                          CommandParameter="{TemplateBinding CommandParameter}"/>
                            <KeyBinding Key="Enter"
                                        Command="{TemplateBinding Command}"
                                        CommandParameter="{TemplateBinding CommandParameter}"/>
                            <KeyBinding Key="Space"
                                        Command="{TemplateBinding Command}"
                                        CommandParameter="{TemplateBinding CommandParameter}"/>
                        </Border.InputBindings>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                <fluentIcons:FluentIcon Icon="{TemplateBinding Icon, Converter={StaticResource FluentIconConverter}}"
                                                       FontSize="18"
                                                       Foreground="{DynamicResource Brush.Accent.Primary}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       SnapsToDevicePixels="True"
                                                       UseLayoutRounding="True"/>
                                <TextBlock Text="{TemplateBinding Title}"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid Grid.Row="1">
                                <ContentPresenter x:Name="ContentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  VerticalAlignment="Stretch"
                                                  HorizontalAlignment="Stretch"/>

                                <StackPanel x:Name="LoadingPanel"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Visibility="Collapsed">
                                    <ProgressBar IsIndeterminate="True"
                                                 Width="40"
                                                 Height="4"
                                                  />
                                    <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Common.Loading]}"
                                               Foreground="{DynamicResource Brush.Text.Secondary}"
                                               FontSize="12"
                                               HorizontalAlignment="Center"
                                               Margin="0,8,0,0"/>
                                </StackPanel>

                                <StackPanel x:Name="ErrorPanel"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Visibility="Collapsed">
                                    <fluentIcons:FluentIcon Icon="Alert"
                                                           FontSize="24"
                                                           Foreground="{DynamicResource Brush.Accent.Warning}"
                                                           HorizontalAlignment="Center"
                                                           SnapsToDevicePixels="True"
                                                           UseLayoutRounding="True"/>
                                    <TextBlock Text="{TemplateBinding ErrorMessage}"
                                               Foreground="{DynamicResource Brush.Text.Secondary}"
                                               FontSize="12"
                                               TextWrapping="Wrap"
                                               TextAlignment="Center"
                                               MaxWidth="200"
                                               Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsLoading" Value="True">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="LoadingPanel" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ErrorPanel" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="ErrorMessage" Value="{x:Null}">
                            <Setter TargetName="ErrorPanel" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsLoading" Value="False"/>
                                <Condition Property="ErrorMessage" Value="{x:Null}"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="LoadingPanel" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="ErrorPanel" Property="Visibility" Value="Collapsed"/>
                        </MultiTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ErrorMessage, Converter={StaticResource NullToBoolConverter}}" Value="True">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="LoadingPanel" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="ErrorPanel" Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
