<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="AccentSwatchBase" TargetType="{x:Type Ellipse}">
        <Setter Property="Width" Value="{DynamicResource AccentSwatchSize}"/>
        <Setter Property="Height" Value="{DynamicResource AccentSwatchSize}"/>
        <Setter Property="Stroke" Value="Transparent"/>
        <Setter Property="StrokeThickness" Value="2"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="AutomationProperties.Name" Value="Accent color swatch"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentSwatchInteractive" TargetType="{x:Type Ellipse}" BasedOn="{StaticResource AccentSwatchBase}">
        <Setter Property="Opacity" Value="0.85"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1"/>
                <Setter Property="Stroke" Value="{DynamicResource Brush.Swatch.Stroke}"/>
                <Setter Property="Effect" Value="{DynamicResource Glow.ButtonHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AccentSwatchSelected" TargetType="{x:Type Ellipse}" BasedOn="{StaticResource AccentSwatchBase}">
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="Stroke" Value="{DynamicResource Brush.Swatch.Stroke}"/>
        <Setter Property="StrokeThickness" Value="3"/>
        <Setter Property="Effect" Value="{DynamicResource Glow.Selection}"/>
    </Style>

</ResourceDictionary>