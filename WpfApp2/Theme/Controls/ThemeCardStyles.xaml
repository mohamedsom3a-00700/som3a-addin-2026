<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <CubicEase x:Key="ThemeCardEase" EasingMode="EaseOut"/>

    <!-- Theme Card Base (unselected) -->
    <Style x:Key="ThemeCardBase" TargetType="{x:Type Border}">
        <Setter Property="Width" Value="{DynamicResource ThemeCardWidth}"/>
        <Setter Property="Height" Value="{DynamicResource ThemeCardHeight}"/>
        <Setter Property="CornerRadius" Value="{DynamicResource CardRadius}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Theme Card Normal State -->
    <Style x:Key="ThemeCard" TargetType="{x:Type Border}" BasedOn="{StaticResource ThemeCardBase}">
        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{DynamicResource Shadow.Card}"/>
        <Setter Property="Opacity" Value="0.85"/>
    </Style>

    <!-- Theme Card Hover Trigger -->
    <Style x:Key="ThemeCardInteractive" TargetType="{x:Type Border}" BasedOn="{StaticResource ThemeCardBase}">
        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect" Value="{DynamicResource Shadow.Card}"/>
        <Setter Property="Opacity" Value="0.85"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                To="1" Duration="0:0:0.15"
                                EasingFunction="{StaticResource ThemeCardEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                To="1.02" Duration="0:0:0.15"
                                EasingFunction="{StaticResource ThemeCardEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                To="1.02" Duration="0:0:0.15"
                                EasingFunction="{StaticResource ThemeCardEase}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                To="0.85" Duration="0:0:0.15"
                                EasingFunction="{StaticResource ThemeCardEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                To="1" Duration="0:0:0.15"
                                EasingFunction="{StaticResource ThemeCardEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                To="1" Duration="0:0:0.15"
                                EasingFunction="{StaticResource ThemeCardEase}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Theme Card Selected State -->
    <Style x:Key="ThemeCardSelected" TargetType="{x:Type Border}" BasedOn="{StaticResource ThemeCardBase}">
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Effect" Value="{DynamicResource Glow.ThemeCard.Selected}"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ToggleButton-based Theme Card (combines interactive + selected) -->
    <Style x:Key="ThemeCardToggleStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Width" Value="{DynamicResource ThemeCardWidth}"/>
        <Setter Property="Height" Value="{DynamicResource ThemeCardHeight}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="CardBorder"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True"
                            CornerRadius="{DynamicResource CardRadius}"
                            BorderBrush="{DynamicResource CardStrokeBrush}"
                            BorderThickness="1"
                            Opacity="0.85">
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <Border.RenderTransformOrigin>
                            <Point X="0.5" Y="0.5"/>
                        </Border.RenderTransformOrigin>
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="{DynamicResource CardHoverBorder}"/>
                            <Setter TargetName="CardBorder" Property="BorderThickness" Value="1"/>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1.02" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1.02" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.85" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="CardBorder" Property="BorderThickness" Value="2"/>
                            <Setter TargetName="CardBorder" Property="Opacity" Value="1"/>
                            <Setter TargetName="CardBorder" Property="Effect" Value="{DynamicResource Glow.ThemeCard.Selected}"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1.02" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1.02" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="CardBorder"
                                            Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1" Duration="0:0:0.15"
                                            EasingFunction="{StaticResource ThemeCardEase}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>